import 'package:flutter/material.dart';

class ReportInventoryWidget extends StatelessWidget {
  const ReportInventoryWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}